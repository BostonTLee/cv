#!/bin/sh

compile_cv() {
	pdflatex cv.tex
}

compile_resume() {
	resume_basename='resume'
	cat cv.tex | sed '/CV-ONLY/d' > "$resume_basename.tex"
	pdflatex "$resume_basename.tex"
}

if [ "$1" = "cv" ]; then
	compile_cv
fi
if [ "$1" = "resume" ]; then
	compile_resume
fi
